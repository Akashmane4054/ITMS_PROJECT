spring:
  application:
    name: user-service

  cloud:
    config:
      uri: http://localhost:9001/config-server
      fail-fast: true

  profiles:
    active: dev   # assuming your config-server has user-service-dev.yml
