spring:
  application:
    name: assessment-service
  profiles:
    active: sit
  jmx:
    default-domain: assessment-service

---
spring:
  config:
    activate:
      on-profile: sit
  cloud:
    config:
      uri: http://localhost:9001/config-server
server:
  servlet:
    context-path: /assessment-service
  port: 9021

