spring:
  application:
    name: health-company-service
  profiles:
    active: sit
  jmx:
    default-domain: health-company-service

---
spring:
  config:
    activate:
      on-profile: sit
  cloud:
    config:
      uri: http://localhost:9001/config-server
server:
  servlet:
    context-path: /health-company-service
  port: 9005        

   
      