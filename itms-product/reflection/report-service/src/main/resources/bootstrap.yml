spring:
  application:
    name: report-service
  profiles:
    active: sit
  jmx:
    default-domain: report-service

---
spring:
  config:
    activate:
      on-profile: sit
  cloud:
    config:
      uri: http://localhost:9001/config-server
server:
  servlet:
    context-path: /report-service
  port: 9888

